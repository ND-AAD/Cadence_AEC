// ─── useNavigation Hook ───────────────────────────────────────────
// Convenience re-export of the NavigationContext hook.
// Components import from here rather than reaching into context/ directly.

export { useNavigationContext as useNavigation } from "@/context/NavigationContext";
